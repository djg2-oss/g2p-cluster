# Agent G2P — optional video only (NOT required for core intelligence)
# Never commit real keys. Never use VITE_ prefix.

RUNPOD_API_KEY=
RUNPOD_VIDEO_ENDPOINT_ID=

# Music later (separate endpoint — do not reuse video endpoint)
# RUNPOD_MUSIC_ENDPOINT_ID=

# Dual-engine cluster edge (optional). Example local:
# VITE_CLUSTER_API=http://127.0.0.1:8080
VITE_CLUSTER_API=
